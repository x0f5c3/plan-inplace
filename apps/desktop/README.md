# Plan InPlace Desktop (Tauri)

This folder contains the Tauri desktop shell for the existing web app.

## Run in desktop dev mode

```bash
npm run tauri:dev
```

## Build desktop binaries

```bash
npm run build:desktop
```
